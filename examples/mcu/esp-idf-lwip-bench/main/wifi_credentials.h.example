#ifndef HPP_GUARD_PLEXUS_EXAMPLE_WIFI_CREDENTIALS_H
#define HPP_GUARD_PLEXUS_EXAMPLE_WIFI_CREDENTIALS_H

// Copy this file to wifi_credentials.h in the same directory and fill in the real values.
// wifi_credentials.h is gitignored and must never be committed.
#define WIFI_SSID     "your-ssid-here"
#define WIFI_PASSWORD "your-password-here"

#endif
