# Projecting a typed topic to CSV on stdout via node.log<Codec, Projection>. Needs only the
# header-only inproc backend, so it builds unconditionally.
plexus_example(logging_example plexus::plexus plexus::inproc)
