# Over-limit exceptions

The single, complete list of every unit that is sanctioned to exceed the size ceiling
in `conventions.md` (functions <=25 lines, files <=200 lines, comments included). An
exception is earned only when a unit is one cohesive whole that splitting would *harm* —
scattering shared state across files or forcing an artificial-purity layer.

A row here is the sole sanction for an over-limit unit — there is no in-code marker; the
justification is bookkeeping and bookkeeping never lives in the code. The size gate fails
the build on any unlisted overage, and on any stale row: one whose unit has dropped back
under the limit, or that names no scanned file at all. There are no silent exceptions.

| Path | Kind | Justification |
| ---- | ---- | ------------- |
| lib/plexus-api/include/plexus/node.h | file | One cohesive public facade; the typed member factories and the private endpoint seams share the engine + subscription/peer/served-fqn state, so splitting the surface scatters that shared state (the upgrade-wiring glue is extracted to detail/node_upgrade_wiring.h). Also covers `endpoint_seam_for` (over the function ceiling, 0 cognitive): one captureless-lambda seam table binding every private *_seam verb to the concrete Policy in one place; splitting it scatters that single seam wiring. |
| lib/plexus-core/include/plexus/io/routing_engine.h | file | One cohesive node-level engine; the demand-driven public verbs share the borrowed substrate members and the posted observer fan-out, so splitting the API scatters that shared state (the sink-install, sink-maker, and posted-dispatch clusters are extracted to detail/). Also covers `dispatch_lifecycle` (over the function ceiling, 0 cognitive): one snapshot-fan over m_observers that honors a same-turn (un)register without invalidating the in-flight iteration; splitting it scatters that single iteration discipline. |
| lib/plexus-core/include/plexus/io/message_forwarder.h | file | One cohesive pub/sub engine; splitting scatters the shared registry + scratch state (the attach gate and the publish/object fan steps are extracted to detail/forwarder_fanout.h). Also covers `publish_object` (over the function ceiling, 0 cognitive): one balanced addref/fan/release pass over the bus slot for an object carrier; splitting it scatters that single refcount-balance discipline. |
| lib/plexus-core/include/plexus/io/procedure_forwarder.h | file | One cohesive req/res correlation engine; splitting scatters the per-peer outstanding table + the reused scratch/active-request state (the rpc fan + send-frame helpers are extracted to detail/procedure_fanout.h). |
| lib/plexus-core/include/plexus/io/known_peers.h | file | One cohesive heap awareness twin; the direct-put, reported-note, and transitive-retire verbs all mutate the single m_table + m_dropped state under the same per-identity candidate contract its fixed twin mirrors (INV-1), so splitting the surface scatters that shared state. The admission/note/retire DECISION cores are already extracted to detail/route_admission.h (admit_candidate, note_reported_row, remove_transitive_row); the remainder is the irreducible per-identity storage state itself. |
| lib/plexus-core/include/plexus/io/fixed_peer_storage.h | file | One cohesive constrained-target awareness twin; the direct-put, reported-note, transitive-retire, and dual (direct + candidate) enumeration verbs all drive the single fixed-capacity slot table under the same per-identity candidate contract its heap twin mirrors (INV-1), so splitting the surface scatters that shared state. The admission/note/retire DECISION cores are already extracted to detail/route_admission.h; the remainder is the irreducible slot-table storage state and its enumerations. |
| lib/plexus-core/include/plexus/io/handshake_fsm.h | file | One transition table over a single handshake transcript; the on_* events and resolve helpers all advance the shared m_state/m_peer_id/m_inbound_pending/m_complete_emitted transcript, so splitting them scatters that shared state (the wire/config types are extracted to handshake_protocol.h). |
| lib/plexus-core/include/plexus/io/peer_session.h | file | One cohesive per-peer bridge; the public seam setters and the receive/staleness/teardown lifecycle share the borrowed ctx/negotiator/fsm + the per-incarnation counters and seam members, so splitting the surface scatters that shared state (the FSM-drive, handshake-encode, consumer-register, and deliver helpers are extracted to detail/peer_session_*.h). Also covers `start` (over the function ceiling, 0 cognitive): one wiring of receive-path + router consumers + handshake-timer arm + outbound-begin over the shared session members; splitting it scatters that single start sequence. |
| lib/plexus-core/include/plexus/io/peer_session_registry.h | file | One cohesive multi-peer connection map; the public query/lifecycle verbs all reach the shared m_slots ownership table + the borrowed m_build context, so splitting the surface scatters that shared state (the session-build and seam-wiring helpers are extracted to detail/peer_session_build.h). |
| lib/plexus-core/include/plexus/io/peer_report_emitter.h | file | One cohesive relay-report twin pair; the note/withdraw/replay/reassert verbs and the decline bookkeeping all mutate the shared m_reported/m_seq/m_declined state, and the members-less null twin must mirror that surface exactly so one template parameter threads both twins with no platform branch — splitting the surface or the twins scatters that shared state and the mirror invariant (the null profile must emit zero offering symbols). |
| lib/plexus-shm/include/plexus/shm/broadcast_ring.h | file | One Vyukov-sequenced lock-free ring; the claim/commit/consume cursor protocol + the seq_cst overwrite-vs-pin Dekker handshake are one indivisible whole over the shared header/cells/cursor atomics — the in-class index accessors only bind member state to the layout (in ring_layout.h) and pulling them out threads the ring members through every hot path, scattering the protocol. Also covers its over-ceiling functions (all 0 cognitive): `create` (the one Vyukov cell-init over the sized control/slab spans), `claim` (the reliable-policy free-cell claim over the cursor atomics), and `claim_best_effort` (the overwrite-skip claim scan) — each one indivisible step of the shared cursor protocol; splitting any scatters that protocol. |
| lib/plexus-shm/include/plexus/shm/shm_mux_member.h | file | One cohesive mux_member contract; the dial/listen/can_acquire/companion verbs all drive the OWNED registry (the sole ring-lifecycle owner) + the per-fqn geometry map, so splitting the surface scatters that shared ownership state (the channel, the consumer, the preference hook, and the acquire/resolve glue are extracted to sibling + detail/ headers). |
| lib/plexus-shm/include/plexus/shm/shm_topic_registry.h | file | One cohesive ring-lifecycle owner; the acquire/release/sink/drain verbs and the nested entry all share the m_entries ownership table + the per-entry notifier-arm/teardown ordering, so splitting the surface scatters that shared lifecycle state (the result types are extracted to shm_acquire_result.h, the open/mint/join glue to detail/shm_topic_open.h). Also covers `acquire` (over the function ceiling, 0 cognitive): the one refcount-bump-or-mint decision over the m_entries table and the per-entry notifier-arm ordering; splitting it scatters that single lifecycle decision. |
| lib/plexus-shm/include/plexus/shm/ring_layout.h | file | Cited cross-process ring layout + named lock-free algorithms (std/parking_lot 3-state word, Vyukov, Dekker mutual-announce) + the cross-process layout invariants; the why is load-bearing (the keep-list) and trimming the citations would lose the ABI/ordering record. |
| backends/plexus-crypto/src/tls_credential.cpp | file | One cohesive OpenSSL credential-load unit; the cert/key/chain parse, validate, and assemble steps share the EVP/X509/BIO handle set and its throw-on-unwind cleanup, so splitting them scatters the cert/key/chain state across files with no readability gain. Also covers the `plexus_tls_verify_callback` body (over the function ceiling, zero cognitive overage): the X509_STORE_CTX -> SSL -> credential -> policy walk with fail-closed at every missing link is one indivisible C-linkage verify decision; splitting it scatters that single fail-closed path. |
| backends/plexus-crypto/src/dtls_channel.cpp | file | One cohesive DTLS BIO/handshake pump; the memory-BIO feed/drain, the SSL_do_handshake drive, and the read/write paths share the one SSL state machine, so splitting them scatters the SSL state across files. Also covers its over-ceiling function bodies (all zero cognitive overage): the ctor (one wiring of the SSL/BIO/cookie/gate member set under throw-on-unwind cleanup), `drain_inbound` (the single memory-BIO read pump over the one SSL/reassembler state), and `try_complete` (the one handshake-finished edge over the shared SSL state) — same cohesive-whole reason as the file; splitting any of them scatters the shared SSL state. |
| lib/plexus-datagram/include/plexus/datagram/detail/udp_reliable_arq.h | file | One cohesive ARQ state machine; the selective-repeat sender window, the reorder receiver, and the RFC-6298 RTO timer all advance the shared sequence/window/ack state, so splitting them scatters the seq/window state. |
| lib/plexus-datagram/include/plexus/datagram/detail/reassembler.h | file | One reassembly window; the fragment-insert, hole-track, and complete-and-deliver steps all operate over the one partial-message buffer, so splitting them scatters the offset/hole state. |
| lib/plexus-wire/include/plexus/wire/frame_reassembler.h | file | One frame-reassembly window; the partial-frame accumulate, the hole/offset track, and the complete-and-emit steps share the one assembly buffer, so splitting them scatters the hole/offset state. Also covers its `feed` body (over the function ceiling, zero cognitive overage): the compact -> accumulate -> hole/offset track -> complete-and-emit sequence is one pass over the single assembly buffer, same cohesive-whole reason as the file; splitting it scatters the hole/offset state. |
| backends/plexus-asio/shm-include/plexus/asio/shm/linux/ring_notifier.h | file | One cohesive futex/eventfd wake protocol; the signal/arm/disarm/park steps share the cross-process generation + park-state words and the eventfd/reactor descriptor, so splitting them scatters the wake/wait/seq state across files. |
| backends/plexus-asio/shm-include/plexus/asio/shm/macos/sem_notifier.h | file | One cohesive named-semaphore + self-pipe wake protocol; the signal/arm/disarm/park steps and the worker share the cross-process generation + park-state words, the semaphore lifecycle, and the self-pipe/reactor descriptor, so splitting them scatters the wake/wait/seq state across files. |
| backends/plexus-asio/include/plexus/asio/udp_channel.h | file | One cohesive UDP byte_channel; the public verbs + the per-peer envelope/dedup/ARQ/reassembly/backpressure state are one whole (the byte_channel concept proof binds the surface together), so splitting the public face scatters that shared state (the send/recv path + ARQ glue are extracted to detail/udp_channel_io.h). |
| backends/plexus-asio/include/plexus/asio/detail/udp_channel_io.h | file | One cohesive UDP I/O state machine; the send-large/fragment, the inbound demux/dedup/reassembly, the ARQ build/fan, and the backpressure drain all advance the shared channel members (the lazily-built ARQ + reassembler, the scratch buffers, the bounded queue), so splitting them further scatters that shared I/O state. Also covers `ensure_arq` (over the function ceiling, 0 cognitive): the one lazy-build of the ARQ closures wiring send/ack/on_data/exhaustion over the shared channel members; splitting it scatters that single closure-wiring. |
| backends/plexus-asio/include/plexus/asio/udp_transport.h | file | One cohesive UDP transport_backend; the listen/dial/close verbs + the config the ctor binds all drive the shared m_server/m_demux/m_dials state (the transport_backend concept proof binds the surface), so splitting the public face scatters that shared state (the accept-loop/dial-resolution/handshake glue is extracted to detail/udp_transport_setup.h). Also covers `dial` (over the function ceiling, 0 cognitive): the one scheme-selects-mode dial that wires the ARQ completion closures and records the per-endpoint dial over m_dials; splitting it scatters that single dial sequence. |
| backends/plexus-asio/include/plexus/asio/udp_server.h | file | One cohesive shared-socket byte pump; the send/recv verbs + the byte-capped outbound queue + the recv buffer + the config the ctor binds are one whole, so splitting the public face scatters that shared socket state (the bind/per-datagram dispatch glue is extracted to detail/udp_server_dispatch.h). |
| backends/plexus-asio/include/plexus/asio/stream_channel.h | file | One byte_channel core 3-into-1-collapsed for every asio stream backend (TCP, AF_UNIX, TLS); the public verbs + the Bootstrap/Traits open seam + the inbound/egress members are one cohesive shared core (the single source of truth the Traits/Bootstrap splits keep each per-backend file lean against), so splitting the surface scatters that shared core (the send-queue drive + read-loop glue is extracted to detail/stream_channel_io.h). |
| backends/plexus-crypto/include/plexus/tls/dtls_transport.h | file | One cohesive secure DTLS transport_backend; the listen/dial/close verbs + the config the ctor binds all drive the shared m_server/m_demux/m_registry/m_cookie state (the transport_backend concept proof binds the surface), so splitting the public face scatters that shared state (the listener/accept/cookie-exchange glue is extracted to detail/dtls_transport_accept.h). |
| backends/plexus-crypto/include/plexus/tls/dtls_channel.h | file | One cohesive DTLS channel surface; the byte_channel verbs + the OpenSSL/BIO/gate/reassembler member state are one whole and the record-pump bodies already live in the .cpp exception (no inline body remains to relocate), so the declaration surface + its member invariants cannot split without scattering the channel's shared SSL/fragment state. |
| lib/plexus-api/include/plexus/recorder.h | file | One cohesive RAII recording handle; the public attach/trigger/flush/pump verbs + the mode-machinery accessors + the schema/preamble builders + the cooperative drain post all drive the shared m_block (the stable-address drain block) under one teardown discipline, so splitting the surface scatters that shared recording state (the drain block + observer-adapter sink are extracted to detail/recorder_drain.h). |
| lib/plexus-core/include/plexus/io/recording/byte_ring.h | file | One cohesive SPSC byte ring; the try_push/drain/evict/peek/copy verbs all advance the shared m_store/m_head/m_tail cursor protocol with release/acquire publication, so splitting them scatters that single-producer/single-consumer cursor state across files. |
| lib/plexus-core/include/plexus/io/recording/record_stream_writer.h | file | One cohesive record encoder; the per-category encode verbs all write through the ONE reused wire::writer over the shared m_scratch and finish through seal()/m_aux, so splitting them scatters that shared scratch + CRC-seal discipline (the framing/CRC helpers are extracted to detail/record_frame.h). |
| backends/plexus-inproc/include/plexus/inproc/inproc_bus.h | file | One cohesive in-process delivery bus; the register/listener/enqueue/deliver verbs + the grown-once packet ring all share the m_channels/m_listeners/m_ring state under the posted-only re-entrancy invariant, so splitting the surface scatters that shared bus state (the per-packet match/dispatch tail is extracted to detail/inproc_dispatch.h). |
| tests/integration/test_typed_inproc_alloc.cpp | file | One cohesive typed zero-alloc matrix; every cell snapshots the one TU-local global new/delete counter (replaceable at most once per binary) around a shared warmed borrow->publish->drain loop, so the cells cannot split across TUs without scattering that single allocation-counter contract. |
| tests/integration/test_hot_path_alloc.cpp | file | One cohesive zero-alloc hot-path matrix; every cell snapshots the one TU-local global new/delete counter (replaceable at most once per binary) around a shared steady-state loop, so the cells cannot split across TUs without scattering that single allocation-counter contract. |
| tests/integration/recording_observer.h | file | One cohesive recording-observer class; every overridden edge method mutates the shared per-peer/per-topic/per-participant tally maps, so the single class body cannot split across files without scattering that shared member state. |
| tests/integration/test_drop_seam_inproc.cpp | file | One cohesive inproc drop+surrender matrix; the drop-cause scenarios all drive the one shared two-node link/observer harness on the single manual virtual clock, so splitting them scatters that shared fixture and clock state. |
| tests/integration/test_forward_splice_no_hol.cpp | file | One cohesive bounded-egress proof; the drop-with-count/no-HOL and the per-origin-FIFO scenarios both drive the one shared controllable-backpressure stall substrate + capture-inner helper, so splitting them across TUs scatters that single fixture. |
| tests/integration/test_forward_envelope_gate.cpp | file | One cohesive cross-envelope proof; the serial drop-with-count-not-teardown arm and the UDP reassemble-then-refragment arm both establish the same splice-time envelope-gate subject over real asymmetric-limit legs, sharing the capture-inner helper and node-id fixture, so splitting them across TUs scatters that single subject. |
| tests/integration/test_forward_serial_e2e.cpp | file | One cohesive relay-forwarding e2e acceptance; the pub/sub, request/response, route-usage, and dual-homed direct-supersedes-relayed switchover cells all drive the shared cold_cluster/dualhome_cluster substrate through the one delivery-ledger + as_bytes/to_string/session_epoch helper set, so splitting the cells across TUs scatters that single fixture and helper set. |
| tests/integration/test_forward_serial_e2e_common.h | file | One cohesive relay-forwarding e2e substrate; the serial+TCP cold_cluster and the all-TCP dual-homed cluster share the id_of/connected/find_participant/named helpers and the borrowed-io lifecycle contract, so splitting the two fixtures across headers scatters that shared substrate. |
| tests/integration/test_drop_coverage.cpp | file | One cohesive drop-observability matrix; every datagram-path drop cause is forced against the one shared engine/reassembler/observer harness, so splitting the per-cause cells scatters that shared fixture. |
| tests/integration/test_security_attach_inproc.cpp | file | One cohesive attach-gate matrix; the policy/posture/install-hook cells all drive the one shared two-peer_session bridge + security-event harness, so splitting them scatters that shared fixture. |
| tests/integration/test_peer_observer_inproc.cpp | file | One cohesive observer lifecycle+readiness matrix; the edge cells all assert against the one shared recording-observer + routing_engine harness on the single manual clock, so splitting them scatters that shared fixture. |
| tests/integration/test_reconnect_inproc.cpp | file | One cohesive single-connection reconnect matrix; the four reconnect-decision cells share the one manual-clock + fixed-RNG backoff harness, so splitting them scatters that shared clock/RNG fixture. |
| tests/integration/test_multipeer_reconnect_inproc.cpp | file | One cohesive N-peer concurrent-drop matrix; the survivor-isolation and surrender cells drive the one shared multi-peer dialer harness on the single manual clock, so splitting them scatters that shared fixture. |
| tests/integration/test_capture_gate.cpp | file | One cohesive capture-gate decision matrix; the per-mode/decimation cells all witness the one shared counting-encode-thunk node-pair harness, so splitting them scatters that shared fixture. |
| tests/integration/test_value_logger.cpp | file | One cohesive value_logger projection matrix; the CSV/JSON-Lines/text + alloc-gate cells share the one decode+projection harness over the typed handle, so splitting them scatters that shared fixture. |
| tests/integration/test_object_route_inproc.cpp | file | One cohesive process-tier object-route matrix; the zero-serialization, unresolved-hash, and reconnect-survival cells share the one two-engine object-route harness, so splitting them scatters that shared fixture. |
| tests/integration/test_downgrade_inproc.cpp | file | One cohesive downgrade/posture-mismatch refusal matrix; the both-ways refusal cells share the one transcript-bound two-peer_session fail-closed harness, so splitting them scatters that shared fixture. |
| tests/integration/test_aead_fragment.cpp | file | One cohesive AEAD-over-fragment composition matrix; the in-order, reorder, and forged-tag cells share the one seal+split+reassembler harness on the inproc test clock, so splitting them scatters that shared fixture. |
| tests/integration/test_recorder_defaults_sweep.cpp | file | One cohesive recorder-defaults sweep; the per-payload recall cells and the decimation guarantees share the one saturating-burst harness at the shipped ring budget, so splitting them scatters that shared sweep fixture. |
| tests/integration/test_shared_receive_route.cpp | file | One cohesive node-shared receive-route matrix; the install-once, reconnect-survival, and per-session-yield cells share the one routing_engine route harness, so splitting them scatters that shared fixture. |
| tests/integration/test_mcap_transcode.cpp | file | One cohesive flat-stream-to-MCAP transcode round-trip; the capture, transcode, and read-back-mapping assertions share the one live-session-to-mcap-file pipeline, so splitting them scatters that shared pipeline state. |
| tests/integration/test_obs_tap_points_common.h | file | One cohesive data-path tap-point oracle fixture; the manual clock/policy, the engine aliases and id/config helpers, and the two harnesses (the N-subscriber publish fan and the caller/provider RPC pair) are one shared scaffold every tap-point test drives, so splitting the harnesses scatters that shared clock/policy/helper state. |
| tests/integration/test_pcap_transcode.cpp | file | One cohesive flat-stream-to-pcapng transcode round-trip; the single end-to-end capture-transcode-parse pipeline is one whole over a shared captured session, so it cannot split without scattering that pipeline state. |
| tests/integration/test_node_xproc_shm.cpp | file | One cohesive cross-process shared-memory proof; the forked same_host_transports rendezvous + medium assertion is one end-to-end pipeline over a shared /dev/shm region, so it cannot split without scattering that cross-process state. |
| tests/integration/test_tls_channel_backpressure_asio.cpp | file | One cohesive real-TLS bounded-outbox hardening leg; the single congestion=drop/block scenario drives the one shared mutual-TLS loopback + stalled-write harness, so it cannot split without scattering that shared socket/TLS state. |
| tests/integration/test_wire_loss_join.cpp | file | One cohesive cross-node loss-join proof; the single sequence-arithmetic join over two skewed-clock recorded streams is one end-to-end pipeline over a shared relay harness, so it cannot split without scattering that shared state. |
| tests/asio/test_udp_congestion.cpp | file | One cohesive congestion-mode matrix; every cell drives the one shared programmable-relay reliable-datagram channel-pair fixture (with its scripted per-segment loss and the bounded back-pressure queue), so splitting the cells scatters that shared relay + channel-pair fixture state. |
| tests/asio/test_udp_reliable_arq.cpp | file | One cohesive selective-repeat ARQ-over-loss matrix; every cell drives the one shared programmable lossy-relay channel-pair fixture (the drop/reorder/duplicate scripting + the window/RTO config), so splitting the cells scatters that shared relay + ARQ fixture state. |
| tests/asio/test_udp_arq_sweep.cpp | file | One cohesive empirical ARQ numeric sweep; the single {loss x window x RTO} grid scenario shares one seeded probabilistic-loss relay harness and its captured result table, so splitting it scatters that shared sweep fixture and the contiguous evidence record. |
| tests/node/test_typed_fastpath.cpp | file | One cohesive fast-path identity+flip matrix; both cells share the one codec + two-node net harness (the encode-count delta and object-address identity witness), so splitting them across TUs scatters that shared fixture into near-empty per-cell shells. |
| tests/node/test_borrowed_executor_bridge.cpp | file | One cohesive borrowed-executor lifecycle gate; the three assertion families all drive the one shared host struct that owns the executor/discovery/transports and embeds the borrowing nodes, so splitting them scatters that shared single-substrate fixture. |
| tests/forwarder/test_egress_priority.cpp | file | One cohesive egress-priority scheduling matrix; every cell drives the one shared backpressured-destination forwarder harness (the stalled-channel band scheduler + drop-event taps), and that shared fixture preamble alone exceeds the file ceiling, so the cells cannot split across TUs without scattering that one harness into over-budget shells. |
| tests/shm/test_medium_coordinator.cpp | file | One cohesive medium-coordinator decision matrix; every cell drives the one shared stub_registry + fake_channel + gate/receive recorder harness (the engine-owned coordinator over its injected gates), so splitting the cells scatters that shared recorder-bound fixture into near-empty per-cell shells. |
| tests/shm/test_shm_wire_fallback.cpp | file | One cohesive wire_fallback per-message routing proof; the cross-process cells share the one live shm broker + forwarder fan-out harness and its per-channel tap, and that shared fixture preamble alone exceeds the file ceiling, so the cells cannot split across TUs without scattering that one harness into over-budget shells. |
| tests/shm/test_shm_large_message.cpp | file | One cohesive large-message live-mux round-trip matrix; the per-notifier-variant cells share the one cross-process publisher/subscriber mux-acquire harness, and that shared fixture preamble alone exceeds the file ceiling, so the cells cannot split across TUs without scattering that one harness into over-budget shells. |
| tests/shm/test_shm_registry.cpp | file | One cohesive topic-registry lifecycle matrix; the acquire/release/teardown cells share the one stub-broker + recording-notifier registry harness, and that shared fixture preamble alone exceeds the file ceiling, so the cells cannot split across TUs without scattering that one harness into over-budget shells. |
| tests/shm/test_shm_mux.cpp | file | One cohesive mux-join selection matrix; the member-satisfies / prefer-hook / fallback cells share the one stub-broker mux-composition harness, and that shared fixture preamble alone exceeds the file ceiling, so the cells cannot split across TUs without scattering that one harness into over-budget shells. |
| tests/shm/test_shm_wire_fallback_node.cpp | file | One cohesive wire_fallback production-seam matrix; the per-mode prefer-hook cells share the one node-declare-path shm broker + mux harness, and that shared fixture preamble alone exceeds the file ceiling, so the cells cannot split across TUs without scattering that one harness into over-budget shells. |
| tests/shm/posix_test_shm_dual_delivery.cpp | file | One cohesive dual-delivery cross-process proof; the single end-to-end SHM-ring + wire-loopback round-trip is one pipeline over a shared converged region and reactor bridge, so it cannot split without scattering that shared cross-process pipeline state. |
| tests/shm/test_shm_notifier_bridge.cpp | file | One cohesive wakeup->reactor bridge matrix; the cells share the one forked producer + io_uring-futex reactor-bridge harness over a converged region, and that shared fixture preamble plus its produce helpers cannot split across TUs without scattering that shared cross-process bridge state into near-empty per-cell shells. |
| tests/shm/test_shm_geometry_defaults.cpp | file | One cohesive safe-defaults + fail-closed geometry matrix; the cells share the one heap-backed stub-broker registry harness, so splitting them scatters that shared broker fixture into near-empty per-cell shells. |
| tests/shm/test_shm_ring_memory_query.cpp | file | One cohesive ring-memory-cost query matrix; the cells share the one recording stub-broker registry harness that captures the requested slab sizes, so splitting them scatters that shared recording-broker fixture into near-empty per-cell shells. |
| tests/shm/test_shm_bilateral.cpp | file | One cohesive bilateral demand-driven convergence proof (xproc); the cells share the one two-process independent-acquire harness over a converged named ring, and that shared fixture preamble cannot split across TUs without scattering that shared cross-process state. |
| tests/handshake/test_handshake_fsm.cpp | file | One cohesive FSM transition matrix over a single shared fixture; the total (state x event) enumeration is one indivisible TEST_CASE and the zero-alloc step binds the single-TU alloc_counter, so splitting scatters the matrix and the one allocation contract. |
| tests/perf/typed_inproc_rig.cpp | file | One cohesive measurement rig bound to the single-TU alloc_counter; every cell reports steady-loop heap behavior against the one program-wide operator-new override and shares the one net/result harness + main report driver, so splitting it across TUs scatters that single-TU allocation contract and the one measurement program. |
| tests/perf/recorder_sweep.cpp | file | One cohesive measurement rig bound to the single-TU alloc_counter; both sweeps share the one net/sink harness + median drivers + main report and the program-wide operator-new override, so splitting it across TUs scatters that single-TU allocation contract and the one measurement program. |
| tests/crypto/test_anti_replay_window_sweep.cpp | file | One cohesive empirical anti-replay-width sweep; both cells share the one fixed-seed LCG + reordered-schedule + reference-window harness, and that shared sweep fixture dominates the file, so splitting the two cells scatters it into near-empty shells. |
| tests/dtls/test_dtls_teardown.cpp | file | One cohesive DTLS teardown-discipline E2E matrix; every cell drives the one shared raw-OpenSSL-client + server-channel + demux loopback harness (whose live-handshake preamble alone exceeds the file ceiling), so splitting the cells scatters that one harness. |
| tests/tls/test_tls_trust.cpp | file | One cohesive TLS trust/pinning E2E matrix; every cell drives the one shared ephemeral-cert + mutual-TLS loopback harness (whose credential + live-handshake preamble alone exceeds the file ceiling), so splitting the cells scatters that one harness. |
| tests/tls/test_tls_seam.cpp | file | One cohesive TLS transport-seam E2E matrix; every cell drives the one shared ephemeral-cert + mutual-TLS transport/loopback harness (whose policy + live-handshake preamble alone exceeds the file ceiling), so splitting the cells scatters that one harness. |
| tests/tls/test_tls_mux.cpp | file | One cohesive TLS-over-mux E2E matrix; every cell drives the one shared ephemeral-cert + mux-face loopback-pair harness (whose identity + quintet-member preamble alone exceeds the file ceiling), so splitting the cells scatters that one harness. |
| lib/plexus-discovery/include/plexus/discovery/multicast_discovery.h | file | One cohesive IPv4 multicast discovery leaf; the advertise/browse/stop/on_inbound/emit verbs all drive the shared borrowed-socket + timer + options + flood-cap + jitter + reused-announcement-scratch state, and the universe reject additionally shares the parsed-once local pattern + its cached concreteness + the label-derived uint32, so splitting the surface scatters that shared per-leaf state. |
| lib/plexus-core/include/plexus/graph/fixed_topic_storage.h | file | One cohesive constrained-target topic backend; the upsert/remove/scan verbs and the inline-owned entry (the topic name + distinct type names at their wire lids) are one flat dep-free array over the shared entries/count state, and it is the byte-identical INV-1 twin of std_map_topic_storage.h — splitting one twin breaks that symmetry — so splitting scatters the shared array-scan state. |
| tests/discovery/test_multicast_discovery_universe.cpp | file | One cohesive universe-match matrix; every cell (foreign-drop, same-universe resolve, wildcard positive/negative, concrete fast-path parity, default-never-match-all, malformed-fail-closed, and the real emit->inbound loop) drives the one shared fake-datagram-socket fixture + the universe option/announcement helpers, so splitting the cells scatters that shared fixture. |
| tests/support/graph_change_inproc.h | file | One cohesive graph-change inproc harness; the recording graph observer plus the two-node routing_engine/known_peers/inproc substrate scaffold are one shared fixture every graph-change oracle (coalescing, teardown, payload, reentrancy, alloc) drives, so splitting it scatters that shared engine/observer state. |

## Function exceptions

Functions over the 25-line ceiling (all 0 cognitive-complexity overage) whose OWNING FILE is
within the 200-line ceiling, so the file carries no `// over-limit:` marker or file row. Each is
suppressed at the site with a function-scoped `// NOLINTNEXTLINE(readability-function-size)` and
registered here. These rows name the FUNCTION in the first column (never a path), so the file-size
gate's file-marker/row reconciliation never reads them as file rows — the file-level 76-marker
cross-check stays intact. A function earns this exception only when it is one cohesive whole that
splitting into one-shot helpers would HARM (the artificial-purity layer the conventions forbid);
a flat 0-cognitive sequential body is the canonical case.

| Function | File:line | Lines (ceiling 25) | Justification |
| -------- | --------- | ------------------ | ------------- |
| `install_same_host_upgrade` | lib/plexus-api/include/plexus/detail/node_upgrade_wiring.h:72 | 28 | One per-member install of the companion-ring MINT + RECEIVE gates into the engine coordinator; a relocation of the node's per-member body, splitting it scatters the paired-gate wiring. |
| `value_logger` (ctor) | lib/plexus-api/include/plexus/value_logger.h:60 | 33 | One linear ctor: the two codec/projection static_asserts then the resolve-identity / qos / adapters / subscriber-register setup over the shared m_state; splitting scatters that single construction. |
| `unix_listener::start` | backends/plexus-asio/include/plexus/asio/unix_listener.h:99 | 36 | One bind sequence (parse, platform-guard, open/bind/listen, arm the accept loop) over the shared listener members; splitting scatters that single bind path. |
| `attach_for_fanout` | lib/plexus-core/include/plexus/io/detail/forwarder_fanout.h:27 | 60 | One per-topic attach gate (hash, type-id equality, qos admit) reading the forwarder registry/endpoint/default through the forwarder reference; a relocation of the forwarder body, splitting scatters the gate. |
| `wire_member` | lib/plexus-core/include/plexus/io/detail/mux_dispatch.h:34 | 28 | One install of a mux member's four completion sinks each forwarding the wrapped channel/edge to the mux's erased sink; a relocation, splitting scatters the four-sink wiring. |
| `register_session_consumers` | lib/plexus-core/include/plexus/io/detail/peer_session_consumers.h:28 | 89 | One install of the per-peer frame_router consumers (handshake/subscribe/data/forwarded decode + route into the FSM and the identity-carrying forwarders); a relocation of the session body, splitting scatters the consumer set. |
| `deliver_data_with_source` | lib/plexus-core/include/plexus/io/detail/peer_session_deliver.h:37 | 31 | One source-identity-aware deliver that lands the data span on the correct (info / 2-arg / dropped) path attributing the passed source; a relocation of the session body, splitting scatters the deliver decision. The plain and forwarded paths pass the session pin and the trailer origin respectively. |
| `install_routing_sinks` | lib/plexus-core/include/plexus/io/detail/routing_sink_install.h:33 | 56 | One flat sequential wiring of every node-shared sink (transport edges, forwarder drop/data/demand/companion, rpc, liveness/tick) over the shared engine members; a relocation of the ctor body, splitting the run into one-shot helpers scatters the wiring with no readability gain. |
| `read_definitions` | lib/plexus-core/include/plexus/io/recording/record_stream_reader.h:75 | 33 | One linear deserialize of the stream header + schema loop over the single reader cursor; splitting scatters that single decode pass. |
| `recover` | lib/plexus-core/include/plexus/io/recording/record_stream_reader.h:111 | 42 | One front-to-back scan of the data section appending every recovered record and accounting the recovery over the single cursor; splitting scatters that single scan. |
| `topic_open_ring` | lib/plexus-shm/include/plexus/shm/detail/shm_topic_open.h:73 | 39 | One acquire-mode-driven open of a named ring (reclaim_stale vs join_live attach-first/mint-second convergence) over the registry entry; splitting scatters that single open decision. |
| `datagram_on_lower_data` | backends/plexus-crypto/include/plexus/crypto/detail/datagram_aead_path.h:89 | 66 | One inbound datagram AEAD path (overhead check, open, anti-replay, deliver) fail-closed at every step over the shared channel members; splitting scatters that single fail-closed path. |
| `decode_subscribe_request` | lib/plexus-wire/include/plexus/wire/detail/subscribe_codec.h:104 | 29 | One linear decode of the subscribe wire format (fixed prefix, the two uint16-prefixed strings, the optional QoS region) over the single payload cursor; splitting scatters that single decode. |
| `dispatch` | lib/plexus-core/include/plexus/io/frame_router.h:86 | 28 | One frame-type jump table routing each wire::msg_type to its consumer (every case delegates to fire/fire_data); a flat 0-cognitive switch whose length is the case-per-type form, splitting it scatters that single dispatch. |
| `decode_record_body` | lib/plexus-core/include/plexus/io/recording/record_decode.h:134 | 35 | One record-category jump table routing each category to its detail:: decoder over the single reader cursor; a flat 0-cognitive switch whose length is the case-per-category form, splitting it scatters that single dispatch. |
